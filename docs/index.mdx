---
title: sincronizado
description: Hyper-local dev with remote AI execution.
---

sincronizado is a hyper-local development stack that combines local file editing with remote AI execution.

## What it is

Edit locally (VS Code / JetBrains). Run AI + heavy execution on a VPS. Keep latency low. Keep compute high.

## Key features

- Local-first development
- Remote AI execution (OpenCode or Claude Code)
- Zero-lag sync: file sync completes in &lt;500ms via Mutagen
- Interactive setup wizard (`sinc --setup`)
- `sinc` CLI for connect/list/kill/setup
- Session persistence on VPS via tmux
- Auto-reconnect and protocol fallback (ssh / et / mosh)

## Install

### One-liner (recommended)

Linux / macOS:

```bash
curl -fsSL https://sync.micr.dev/install.sh | bash
```

Windows (PowerShell):

```powershell
irm https://sync.micr.dev/install.ps1 | iex
```

Then run:

```bash
sinc --setup
```

From source:

```bash
git clone https://github.com/Microck/sincronizado.git
cd sincronizado
bun install
bun run dev -- --setup
```

## Quick links

- [Quick start](/quick-start)
- [Installation](/installation)
- [VPS setup](/vps-setup)
- [Architecture](/architecture)
- [CLI reference](/cli)
- [Configuration](/configuration)
- [Troubleshooting](/troubleshooting)

## Help

- **LLM Installation Guide**: https://raw.githubusercontent.com/microck/sincronizado/main/INSTALL.md
- GitHub issues: https://github.com/Microck/sincronizado/issues
- OpenCode Discord: https://discord.gg/opencode
